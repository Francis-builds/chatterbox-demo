# Ultravox + ElevenLabs Voicebot Pipeline
# Optimized for L4 (24GB) or T4 (16GB with quantization)

# Ultravox model inference
transformers>=4.45.0
torch>=2.1.0
torchaudio>=2.1.0
accelerate>=0.25.0
bitsandbytes>=0.42.0  # For int8/int4 quantization on T4

# ElevenLabs TTS
elevenlabs>=1.0.0
httpx>=0.25.0

# Audio processing
numpy>=1.24.0
librosa>=0.10.0
soundfile>=0.12.0

# Gradio UI
gradio>=5.0.0

# Streaming / async
websockets>=12.0
